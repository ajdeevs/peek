# Peek

[![npm version](https://img.shields.io/npm/v/peek?style=flat-square)](https://www.npmjs.com/package/@ajdeevs/peek)  

A **modern file listing CLI tool** written in JavaScript.  
Peek lists files and directories with color-coded output based on file type, similar to `ls` or `eza`.

---

## Features

- Color-coded file types using [Chalk](https://www.npmjs.com/package/chalk).  
- Hidden file support with `-a` / `--all` flag.  
- Lightweight, fast, and easy to use.  
- Cross-platform: Linux, macOS, and Windows (Node.js required).

---

## Installation

### From npm

```bash
npm install -g @ajdeevs/peek
